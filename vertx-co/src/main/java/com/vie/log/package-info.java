package com.vie.log;

interface Message {
    /** **/
    String INF_ANNAL = "[ZERO] The logger ( Annal = {0} ) has been selected to record logs.";
    /** **/
    String INF_INJECT = "[ZERO] The inject Annal has not been configured, will select internal.";
}
