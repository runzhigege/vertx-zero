excel:
  config:
    mapping:
      - table: CI_DEPT
        pojo: up.zero.domain.tables.pojos.CiDept
        dao: up.zero.domain.tables.daos.CiDeptDao
        unique:
          - code
          - appId