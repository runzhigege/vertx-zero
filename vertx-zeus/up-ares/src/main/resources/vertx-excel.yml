excel:
  config:
    mapping:
      - table: S_USER
        pojo: cn.vertxup.domain.tables.pojos.SUser
        dao: cn.vertxup.domain.tables.daos.SUserDao
        unique:
          - username
          - sigma